{% extends 'base.html.twig' %}

{% block title %}Nos articles{% endblock %}

{% block body %}

    <h1>{{ post.title }}</h1>
    <p class="small">Article créé le {{ post.createdAt|date }}</p>

    <div>
        {{ post.content|raw }}
    </div>

{% endblock %}
